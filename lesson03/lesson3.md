# lesson3
## 一、指针
int *p		是一个指针变量，可以直接将变量的地址赋给他。
*p		是p这个指针指向的数据，如果在这里赋值，只能给他一个整数。
&p		这是指向整数值的地址存放的内存地址，也就是p变量的地址。

## 二、指针有运算：
假定有指针
int *pa;
int a[10]={19};
pa = &a;
pa + 1		// a[1]的地址
*pa + 1		// 20
*(pa + 1)	// 0