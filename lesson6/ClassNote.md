# ClassNote

1. C语言中没有字符串数据类型，用到字符串就使用字符数组表示。
2. 当遇到一个字符数据时，用不同的格式符号输出数据，得到的结果不一样。如果是它的值是一个字母，在输出它用%c时，他就输出字母，否则他是一个ASCII值如果他不是个字母，输出的可能什么也没有，也可能是空格。例如，如果变量的值是32，他表示一个空格
3. 当使用一个字符串时，如果没有给字符串数组的最后一个元素赋一个结束符号'\0'，那么输出时会出现意外结果。
4. 另外还要注意对字符串的操作要使用相关的函数，并且在使用时要用到<string.h>头文件
5. "a"表示一个字符串，而'a'代表一个字符